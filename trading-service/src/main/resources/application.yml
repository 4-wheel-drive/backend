spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3307/modular1
    driver-class-name: org.mariadb.jdbc.Driver
    username: admin
    password: admin1024
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
server:
  port: 8082
